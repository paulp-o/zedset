# Environment variables for ZedSet
# Copy this file to .env and modify as needed

# Node environment (development/production)
NODE_ENV=development

# Note: Vercel Analytics is automatically enabled in production builds
# No additional configuration needed - it's handled by import.meta.env.PROD
